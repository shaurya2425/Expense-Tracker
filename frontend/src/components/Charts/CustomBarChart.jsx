import React from 'react'
import {
    BarChart,
    Bar,
    XAxis,
    YAxis,
    CartesianGrid,
    Tooltip,
    Legend,
    ResponsiveContainer,
    Cell
} from 'recharts'


function CustomBarChart({data}) {
  return (
    <div>
      CustomBarCharts
    </div>
  )
}

export default CustomBarChart
